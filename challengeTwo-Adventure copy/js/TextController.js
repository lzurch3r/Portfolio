//import {parseJSON} from "./utils.js";
import { npcText, roomText } from "./text.js";

export function buildText(roomId, ) {
  const textArray = [
    getRoomText(roomText,id),
    getNPCText(npcText,id)
  ];
  //textArray[0] corresponds to 
}

function getNPCText(npcText, ) {
  
}